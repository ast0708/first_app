- provide(:title, @user.name)
%h1
	= @user.name
.row 
	%aside.span4
		%section
			= gravatar_for @user
			= @user.name
	.span8
		= if @user.microposts.any?
			%h3
				Microposts
				= @user.microposts.count
			%ol
				= render @microposts
			= will_paginate @microposts